
	(function($){
		$(window).load(function(){
            var amount=Math.max.apply(Math,$("#content-coming li").map(function(){return $(this).outerWidth(true);}).get());
			$("#content-coming").mCustomScrollbar({
				axis:"x",
				theme:"inset",
				advanced:{
					autoExpandHorizontalScroll:true
				},
				scrollButtons:{
					enable:true,
					scrollType:"stepped"
				},
				keyboard:{scrollType:"stepped"},
				snapAmount:amount,
				mouseWheel:{scrollAmount:amount}
			});
            var amount=Math.max.apply(Math,$("#content-1 li").map(function(){return $(this).outerWidth(true);}).get());
			$("#content-1").mCustomScrollbar({
				axis:"x",
				theme:"inset",
				advanced:{
					autoExpandHorizontalScroll:true
				},
				scrollButtons:{
					enable:true,
					scrollType:"stepped"
				},
				keyboard:{scrollType:"stepped"},
				snapAmount:amount,
				mouseWheel:{scrollAmount:amount}
			});
			var amount=Math.max.apply(Math,$("#content-event li").map(function(){return $(this).outerWidth(true);}).get());
			$("#content-event").mCustomScrollbar({
				axis:"x",
				theme:"inset",
				advanced:{
					autoExpandHorizontalScroll:true
				},
				scrollButtons:{
					enable:true,
					scrollType:"stepped"
				},
				keyboard:{scrollType:"stepped"},
				snapAmount:amount,
				mouseWheel:{scrollAmount:amount}
			});
            var amount=Math.max.apply(Math,$("#content-pop li").map(function(){return $(this).outerWidth(true);}).get());
			$("#content-pop").mCustomScrollbar({
				axis:"x",
				theme:"inset",
				advanced:{
					autoExpandHorizontalScroll:true
				},
				scrollButtons:{
					enable:true,
					scrollType:"stepped"
				},
				keyboard:{scrollType:"stepped"},
				snapAmount:amount,
				mouseWheel:{scrollAmount:amount}
			});
            var amount=Math.max.apply(Math,$("#content-rock li").map(function(){return $(this).outerWidth(true);}).get());
			$("#content-rock").mCustomScrollbar({
				axis:"x",
				theme:"inset",
				advanced:{
					autoExpandHorizontalScroll:true
				},
				scrollButtons:{
					enable:true,
					scrollType:"stepped"
				},
				keyboard:{scrollType:"stepped"},
				snapAmount:amount,
				mouseWheel:{scrollAmount:amount}
			});
            var amount=Math.max.apply(Math,$("#content-dance li").map(function(){return $(this).outerWidth(true);}).get());
			$("#content-dance").mCustomScrollbar({
				axis:"x",
				theme:"inset",
				advanced:{
					autoExpandHorizontalScroll:true
				},
				scrollButtons:{
					enable:true,
					scrollType:"stepped"
				},
				keyboard:{scrollType:"stepped"},
				snapAmount:amount,
				mouseWheel:{scrollAmount:amount}
			});
            var amount=Math.max.apply(Math,$("#content-rbs li").map(function(){return $(this).outerWidth(true);}).get());
			$("#content-rbs").mCustomScrollbar({
				axis:"x",
				theme:"inset",
				advanced:{
					autoExpandHorizontalScroll:true
				},
				scrollButtons:{
					enable:true,
					scrollType:"stepped"
				},
				keyboard:{scrollType:"stepped"},
				snapAmount:amount,
				mouseWheel:{scrollAmount:amount}
			});
            var amount=Math.max.apply(Math,$("#content-dj li").map(function(){return $(this).outerWidth(true);}).get());
			$("#content-dj").mCustomScrollbar({
				axis:"x",
				theme:"inset",
				advanced:{
					autoExpandHorizontalScroll:true
				},
				scrollButtons:{
					enable:true,
					scrollType:"stepped"
				},
				keyboard:{scrollType:"stepped"},
				snapAmount:amount,
				mouseWheel:{scrollAmount:amount}
			});
            var amount=Math.max.apply(Math,$("#content-child li").map(function(){return $(this).outerWidth(true);}).get());
			$("#content-child").mCustomScrollbar({
				axis:"x",
				theme:"inset",
				advanced:{
					autoExpandHorizontalScroll:true
				},
				scrollButtons:{
					enable:true,
					scrollType:"stepped"
				},
				keyboard:{scrollType:"stepped"},
				snapAmount:amount,
				mouseWheel:{scrollAmount:amount}
			});
            var amount=Math.max.apply(Math,$("#content-punks li").map(function(){return $(this).outerWidth(true);}).get());
			$("#content-punks").mCustomScrollbar({
				axis:"x",
				theme:"inset",
				advanced:{
					autoExpandHorizontalScroll:true
				},
				scrollButtons:{
					enable:true,
					scrollType:"stepped"
				},
				keyboard:{scrollType:"stepped"},
				snapAmount:amount,
				mouseWheel:{scrollAmount:amount}
			});
            var amount=Math.max.apply(Math,$("#content-staff li").map(function(){return $(this).outerWidth(true);}).get());
			$("#content-staff").mCustomScrollbar({
				axis:"x",
				theme:"inset",
				advanced:{
					autoExpandHorizontalScroll:true
				},
				scrollButtons:{
					enable:true,
					scrollType:"stepped"
				},
				keyboard:{scrollType:"stepped"},
				snapAmount:amount,
				mouseWheel:{scrollAmount:amount}
			});
            var amount=Math.max.apply(Math,$("#content-festivals li").map(function(){return $(this).outerWidth(true);}).get());
			$("#content-festivals").mCustomScrollbar({
				axis:"x",
				theme:"inset",
				advanced:{
					autoExpandHorizontalScroll:true
				},
				scrollButtons:{
					enable:true,
					scrollType:"stepped"
				},
				keyboard:{scrollType:"stepped"},
				snapAmount:amount,
				mouseWheel:{scrollAmount:amount}
			});
            
		});
	})(jQuery);